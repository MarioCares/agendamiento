allowBuilds:
  '@parcel/watcher': true
  '@prisma/client': false
  better-sqlite3: true
  esbuild: true
  workerd: true

# Permit immediate installation of published versions. This avoids release-age
# resolution failures in local development and CI.
minimumReleaseAge: 0
